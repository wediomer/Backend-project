<!-- navbar.ejs -->

<nav class="navbar navbar-expand-lg navbar-dark bg-light mt-4 mb-3 border border-primary border-top-2">
  <div class="container border border-primary border-top-2">
    <div>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse btn btn-link text-decoration-none bg-primary text-light m-1" id="navbarNav">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item <% if (currentPage === 'register') { %>active <% } %>">
            <a class="nav-link btn btn-link text-decoration-none bg-primary border border-primary text-light m-1 " href="/register">Register</a>
          </li>
          <li class="nav-item  <% if (currentPage === 'login') { %>active <% } %>">
            <a class="nav-link btn btn-link text-decoration-none bg-primary text-light m-1" href="/login">Login</a>
          </li>
          <li class="nav-item <% if (currentPage === 'createBlog') { %>active <% } %>">
            <a class="nav-link btn btn-link text-decoration-none bg-primary text-light m-1" href="/createBlog">Create Blog</a>
          </li>
          <li class="nav-item <% if (currentPage === 'homepage') { %>active <% } %>">
            <a class="nav-link btn btn-link text-decoration-none bg-primary text-light m-1" href="/">Homepage</a>
          </li>
          <li class="nav-item">
            <a class="nav-link btn btn-link text-decoration-none bg-primary text-light m-1" href="/logout">Logout</a>
          </li>
        </ul>
        
        
        
      </div>
      
    </div>
    <!-- "Go Back" link -->
    <a href="javascript:history.go(-1)" class="m-2 text-decoration-none btn btn-link bg-primary text-light p-3">Go Back</a>
  </div>
</nav>
